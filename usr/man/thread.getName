NAME
  thread.getName
  
CALLS
  thread.getName(string: threadUid)
  
RETURNS
  string: name 
    OR 
  boolean: false, string: errMsg
  
DESCRIPTION
  Gets the name for a thread. Use it for your own needs to tag, filter or improve user-friendlyness.
  
EXAMPLES
  thread.getName(threadUid)
  thread.getName(thread.getThread().uid)
