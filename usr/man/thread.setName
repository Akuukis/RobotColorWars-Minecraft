NAME
  thread.setName
  
CALLS
  thread.setName(string: threadUid, string: name)
  
RETURNS
  boolean: success[, string: errMsg]
  
DESCRIPTION
  Sets the name for a thread. Use it for your own needs to tag, filter or improve user-friendlyness.
  
EXAMPLES
  thread.setName(threadUid, "TestThread")
  thread.setName(thread.getThread().uid, "MyThread")
