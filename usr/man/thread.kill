NAME
  thread.kill
  
CALLS
  thread.kill(string: threadUid)

RETURNS
  boolean: success[, string: errMsg]
  
DESCRIPTION
  It brutally and instantly kills the thread.
  Tip: use thread.whisper to ask thread to die peacefully so it is able to return values beforehand.

EXAMPLES
  thread.kill(threadUid)
  