NAME
  thread.getAnswer
  
CALLS
  thread.getAnswer(string: threadUid[, undefined: msg])
  
RETURNS
  undefined list: answer
    OR 
  boolean: false, string: errMsg
  
DESCRIPTION
  Gets the answer from a thread, optionally passing a message as parameter.
  The same as thread.ask
  
EXAMPLES
  thread.getAnswer(threadUid)
  thread.getAnswer(threadUid,3)
