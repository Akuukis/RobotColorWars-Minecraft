NAME
  thread.setAlarm
  
CALLS
  thread.setAlarm(string: threadUid[, number: alarm])
  
RETURNS
  boolean: success[, string: errMsg]
  
DESCRIPTION
  Sets the alarm for a thread. Thread will sleep and skip all signals for that period of time. If no alarm is given, then removes alarm.
  
EXAMPLES
  thread.setAlarm(threadUid, 1000)
  thread.setAlarm(threadUid, nil)
