NAME
  thread.getThread
  
CALLS
  thread.getThread([threadUid])
  
RETURNS
  table: properties
    OR 
  boolean: false, string: errMsg
  
DESCRIPTION
  Gets a table with properties of target thread or by default the thread calling that function.
  Use "man thread.properties" to see the list of properties.
  
EXAMPLES
  thread.getThread()
  thread.getThread(threadUid)
